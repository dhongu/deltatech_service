## 19.0.1.1.13 (2026-09-24)

- Consultant sheet (`readme/FISA_CONSULTANT.md`, in Romanian) with 11 screenshots generated by
  `tests/test_screenshots.py` (runs only on request, tag `fise_screenshots`): installation,
  agreement, meter readings and consumption-based invoicing, with the known limitations.
- Romanian translation: wrong terms corrected (e.g. *Removal*, *Agreement Type*, *Meters*).

## 19.0.1.1.12 (2026-09-23)

- Translatable strings in code use `self.env._()` instead of `_()`, the Odoo 19
  convention (pylint-odoo `prefer-env-translation`). The translated messages are
  unchanged.
